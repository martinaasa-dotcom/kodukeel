node_modules
.next/
out/
build/
.env
.env*.local
*.db
*.db-journal
.data/
next-env.d.ts
tsconfig.tsbuildinfo
.DS_Store

# Scratch scripts used while driving the app in a browser.
*.tmp.mjs
.backups/
prisma/data/.cache/

# Cached source files for build-frequency.ts and build-emoji.ts. Regenerated
# by running those scripts; the raw wordlist/emoji-test text has no reason to
# travel with the repo or a deployment.
.frequency-cache/
.emoji-cache/

# Cached Ekilex responses, so a re-harvest costs the service nothing
.ekilex-cache/

# Local before/after screenshots taken while working on a change.
.shots/

# The hosted-mode build scripts/test-signin.mjs makes for itself.
.next-signin/
